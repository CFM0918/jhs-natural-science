# 國中理化/地科教材專案

108 課綱國中自然（理化＋地科，八上～九下會考範圍）加值教材。編輯法沿用《國中數學教材》專案（`C:\Users\SD\國中數學教材\`）。

## 結構
```
理化/
├── Token_Saver_Pro.md   # Agent 系統提示詞（省 token 產生器規範）
├── 新課程目次.md         # 理化/地科 章→節→內容細目，康/翰/南對照＋課綱異動（權威目次）
├── 生物目次.md           # 生物(七年級)目次，補齊會考自然第三塊
├── 01_原始教材HTML/      # 各節互動教材
├── 02_加值成品/          # 每節 5 種產出，分冊(八上/八下/九上/九下/生物)
└── 03_產生器/            # L_<code>.py 內容檔 ＋ pipeline（engine/slides/run，已本機化）
```

## 對照版本
康軒／翰林／南一。取三版共同會考範圍；差異在目次「版本註」欄標示。

## 冊別前綴（L 檔）
`a`生物(七年級) `b`八上 `c`八下 `d`九上 `e`九下。九上 Ch5–7、九下 Ch3–4 為地科。

## 產生器用法（本機）
`cd 03_產生器 && python run.py b1-1` → 依前綴自動輸出到 `02_加值成品/<冊>/`，產 5 種成品。
需 `pip install openpyxl`（已裝）。
PDF 轉檔已移植：`python topdf.py all`（playwright+chromium，已裝），簡報/圖表/摘要/Bloom 皆可轉 PDF；測驗保留 XLSX。
測驗題目為 30 題手做版（每卷10題），補充題庫見 `quiz_extra_<冊>.py`，驗證 `python verify_quiz.py`。
成品導覽：根目錄 `00_教材總目錄.html`、審閱 `00_內容審閱.html`／`00_教學內容審閱.html`。
已驗證：全 103 節 5 成品產出正常，412 份 PDF 全數有效。

## 帳號
jay29872@gmail.com（同數學專案）。
